pyyaml>=6.0.1
pytest>=8.0.0

# sentence-transformers required for the bge_local provider (default):
sentence-transformers>=2.7.0
tiktoken>=0.7.0

# Optional — only needed if you switch back to the openai provider:
# openai>=1.30.0
