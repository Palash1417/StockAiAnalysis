# Root requirements — installed by Render for the FastAPI backend.
# Covers phase_9_api + all phases it imports at runtime
# (phase_4_1 embedder, phase_5 Chroma adapter, phase_6 retrieval,
#  phase_7 generation, phase_8 guardrails).

# --- API server ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0
httpx>=0.27.0

# --- LLM (Groq) ---
groq>=0.9.0

# --- Session store (Redis; SQLite is stdlib) ---
redis>=5.0.0

# --- Embedder (BGE-small local via sentence-transformers) ---
sentence-transformers>=2.7.0

# --- Vector store (Chroma Cloud) ---
chromadb>=0.5.0
